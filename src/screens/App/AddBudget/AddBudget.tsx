import React, { FC } from "react";
import { SafeAreaView, Text } from "react-native";

const AddBudgetScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>AddBudgetScreen</Text>
    </SafeAreaView>
  );
};

export default AddBudgetScreen;
