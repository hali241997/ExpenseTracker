import React, { FC } from "react";
import { SafeAreaView, Text } from "react-native";

const TransactionEditExpenseScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>TransactionEditExpenseScreen</Text>
    </SafeAreaView>
  );
};

export default TransactionEditExpenseScreen;
