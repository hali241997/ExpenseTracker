import React, { FC } from "react";
import { SafeAreaView, Text } from "react-native";

const EditExpenseScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>EditExpenseScreen</Text>
    </SafeAreaView>
  );
};

export default EditExpenseScreen;
