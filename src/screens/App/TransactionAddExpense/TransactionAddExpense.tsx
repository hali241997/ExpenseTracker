import React, { FC } from "react";
import { SafeAreaView, Text } from "react-native";

const TransactionAddExpenseScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>TransactionAddExpenseScreen</Text>
    </SafeAreaView>
  );
};

export default TransactionAddExpenseScreen;
