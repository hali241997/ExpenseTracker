import { Text } from "@components/index";
import React from "react";
import { ScrollView } from "react-native";

const JointWallet = () => {
  return (
    <ScrollView>
      <Text>Personal</Text>
    </ScrollView>
  );
};

export default JointWallet;
