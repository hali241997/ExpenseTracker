import { Col, Header } from "@components/index";
import React, { FC } from "react";

const CreateWalletScreen: FC = () => {
  return (
    <Col>
      <Header title="Create Wallet" withBackButton />
    </Col>
  );
};

export default CreateWalletScreen;
